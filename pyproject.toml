[project]
name = "interview-demo"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.136.3",
  "mcp>=1.27.2",
  "numpy>=2.4.0",
  "pyyaml>=6.0.3",
  "sentence-transformers>=5.5.1",
]
